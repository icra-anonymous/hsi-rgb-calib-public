[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hsi-rgb-calib"
version = "0.1.0"
description = "Calibration toolkit for rigid pushbroom HSI line-scan camera with OAK-D S2 RGB-D camera rig"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "HSI-RGB Calibration Team"}
]
keywords = ["hyperspectral", "calibration", "rgb", "oak-d", "line-scan", "pushbroom"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Image Processing",
]

dependencies = [
    "numpy>=1.21.0",
    "opencv-python>=4.5.0",
    "pyyaml>=6.0",
    "scipy>=1.7.0",
    "rich>=12.0.0",
    "matplotlib>=3.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[project.scripts]
hsi-rgbd-calib = "hsi_rgbd_calib.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
